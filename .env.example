# Public site URL (for metadata)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Supabase (optional — site works with demo content until these are set)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Admin password for publishing content (set a strong password!)
ADMIN_PASSWORD=change-me-before-deploy

# Optional: OpenAI for "Ask Project_Steam" grounded answers
OPENAI_API_KEY=
